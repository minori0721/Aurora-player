# 🎵 Aurora Player (极光播放器) preview.ver

**在线体验**：[https://minori0721-test-1.aurora-player.pages.dev/](https://minori0721-test-1.aurora-player.pages.dev/)
或：[https://player.minori0721.dpdns.org/](https://player.minori0721.dpdns.org/)

> 一个基于 Web Audio API 的现代网页音乐播放器，专为 **无缝循环 (Seamless Loop)**、**高保真音频** 及 **跨平台体验** 设计。

## ✨ 项目简介

**Aurora Player** 是一个纯前端、单文件的音乐播放器。它摆脱了传统 HTML5 `<audio>` 标签的限制，直接利用浏览器的 AudioContext 进行音频解码与播放。

本项目致力于解决两大痛点：**游戏 BGM 的无缝循环播放** 与 **高质量的在线音乐体验**。通过内置的原生二进制解析器和网易云音乐深度集成，它既能作为本地离线播放器，也能变身为强大的在线流媒体客户端。

## 🚀 核心功能

### ☁️ 网易云音乐深度集成 (Enhanced)

不再局限于本地文件，Aurora Player 现已接入完整的网易云音乐生态：

* **音乐内容浏览器 (NEW!)**：点击「☁️ 云音乐」即可按综合、歌曲、专辑和歌手分类搜索，并在歌曲、专辑、歌手之间直接跳转。
* **专辑与歌手页面 (NEW!)**：专辑页提供完整曲目、顺序/随机播放和收藏；歌手页提供简介、热门歌曲、全部歌曲、专辑/EP、MV 和关注。
* **浏览历史与上下文队列 (NEW!)**：支持页面前进/后退、深链接和搜索状态恢复；从专辑或歌手页面播放时会自动建立对应队列并继续播放后续曲目。
* **无损音质下载 (NEW!)**：支持直接下载歌曲文件。系统会自动检测并提供 **标准 (128k) / 较高 (192k) / 极高 (320k) / 无损 (FLAC) / Hi-Res** 等多种音质选项（视账号权益而定）。
* **账号登录**：支持 **APP 扫码登录**，同步加载「我创建的歌单」和「我收藏的歌单」，解锁 VIP 音质，并可收藏专辑或关注歌手。
* **歌单导入**：支持粘贴网易云歌单链接或 ID，一键导入整张歌单到播放列表。
* **三段式歌词切换 (NEW!)**：独家支持 **仅原文 / 原文+翻译 / 原文+罗马音** 三段式循环切换，完美适配日语歌曲学习与欣赏需求。
* **逐字同步歌词 (NEW!)**：深度对接逐字歌词 (YRC) 引擎，实现 **Apple Music 风格的逐字渐进高亮** (逐字点亮动画)，给您极致的视觉律动感。

### 🎧 专业音频回放 & 体验

* **全格式支持**：完美播放 MP3, FLAC, OGG, WAV, M4A 等主流格式。
* **按需渲染**：暂停且关闭视觉特效时停止动画帧；普通播放以较低频率刷新时间和歌词，视觉特效开启后恢复流畅绘制，页面隐藏时暂停视觉任务。
* **结构化播放列表恢复**：在线播放列表和当前索引保存到 IndexedDB 的 `player-state` 存储区，不再从显示文本反推歌名；旧版 `localStorage` 数据会自动迁移。
* **无缝循环 (Seamless Loop)**：单曲循环直接在 Web Audio 音频线程内按 `0 → duration` 回跳；启用 A/B 或 `.sli` 后则按自定义采样点循环，不需要重新请求或解码歌曲。
* **文件夹递归导入 (NEW!)**：可以直接拖入包含音频、`.sli` 和 `.lrc` 的完整原声带目录，扫描完成后一次性生成播放列表。
* **SLI 自动配对 (NEW!)**：原生支持 `.sli` (Sound Loop Information)，可识别 `曲名.sli`、`曲名.wav.sli`、`曲名.ogg.sli` 和大小写混合扩展名，并按目录与文件名安全配对。
* **防串曲加载 (NEW!)**：本地文件、网易云音频、歌词和循环点共用播放加载令牌，快速切歌时旧任务不会覆盖当前歌曲。

### 📱 响应式设计 (Mobile Ready)

* **多端适配**：精心设计的响应式布局，在 **手机、平板** 和 **桌面端** 均有完美表现。
* **手势操作**：移动端支持底部面板滑出、触摸拖动进度条。
* **键盘与无障碍**：主要移动端触点不小于约 44px，播放列表支持 Enter/Space；弹层会锁定背景、限制 Tab 焦点并在关闭后恢复原焦点。
* **自适应菜单**：
* 桌面端：显示完整工具栏。
* 移动端：自动折叠至「⚙️ 高级」菜单（包含 Debug、智能分析、循环设定、特效切换等）。



### 🔍 智能循环分析

* **三档匹配算法**：支持「速度优先 / 均衡推荐 / 准确优先」，可按歌曲复杂度和等待时间选择匹配策略。
* **智能对齐**：只需粗略设定 A/B 点，算法会自动在附近寻找最佳无爆音接缝。
* **后台分析**：默认使用 Web Worker 执行智能匹配，降低长时间分析时主界面卡住的概率。
* **一键导出**：支持将分析结果导出为 `.sli` 文件。

### 🎨 沉浸式 UI

* **流体极光背景**：使用 `ColorThief` 提取专辑封面主色调，生成动态流体背景。
* **动态特效**：内置多种 Canvas 可视化效果：
* ✨ **无特效** (纯净模式)
* ❄️ **凛冬飞雪** (随低音鼓点加速)
* 📊 **律动频谱**
* 🌊 **极光波浪**



## 📖 使用指南

### 方式一：本地播放 (拖拽投喂)

1. **文件支持**：直接将 `.mp3`, `.flac`, `.ogg`, `.wav`, `.m4a` 等音频文件或整个文件夹拖入窗口。
2. **递归扫描**：拖入文件夹时会读取其中的子目录，等扫描完成后再一次性导入，不会边扫描边播放。
3. **配套文件**：同批或后续补充的歌词 (`.lrc`) 与循环信息 (`.sli`) 会重新匹配整个播放列表，不再只作用于当前歌曲。
4. **批次规则**：包含音频的新批次会替换当前播放列表；只有 `.sli` / `.lrc` 的批次会补充当前列表。空列表时先导入的伴随文件会暂存到下一批本地音频。
5. **匹配状态**：成功绑定 SLI 的歌曲会在播放列表显示 `∞` 标记；导入后会显示音频、SLI、匹配、未匹配、歧义和无效数量。
6. **浏览器兼容**：优先使用标准文件系统接口，并兼容 Chromium/Safari 的旧目录接口；浏览器无法展开目录时会明确提示改为选择目录内文件。
7. **快捷键**：
* `Space`: 播放/暂停
* `← / →`: 快退/快进
* `↑ / ↓`: 音量调节

快捷键只在页面非交互区域生效。焦点位于输入框、按钮、选择器、进度条、链接或弹层内时，保留控件原生键盘行为。



### 方式二：网易云在线模式

1. 点击顶部工具栏的 **「☁️ 云音乐」** 按钮打开面板。
2. **搜索与浏览**：输入关键词后可切换「综合 / 歌曲 / 专辑 / 歌手」，点击歌手名或专辑封面进入对应页面。
3. **连续播放**：从搜索结果、专辑或歌手页面选择歌曲后，播放器会使用当前页面内容建立队列；浏览器面板保持打开，底部迷你播放器可控制播放。
4. **历史导航**：页面地址使用 `#music/...` Hash 路由，可刷新、前进、后退或直接打开专辑与歌手深链接。关闭面板不会停止音乐，再次打开会恢复最后浏览位置。
5. **扫码登录**：点击「扫码登录网易云账号」，使用网易云 APP 扫码，即可解锁歌单、高音质、专辑收藏和歌手关注。
6. **下载音乐**：播放歌曲时，点击控制栏右侧的 **下载图标** (⬇️)，选择心仪的音质进行下载。

音乐浏览器与下载窗口使用统一弹层行为：打开后背景不可操作，`Tab / Shift+Tab` 不会跳出弹层，`Escape`、关闭按钮和背景点击使用同一关闭流程。音乐浏览器根页的返回按钮只关闭面板，不会把 Aurora 页面退到 `about:blank`；浏览器前进仍可重新进入刚关闭的音乐页。

### 离线依赖、缓存与隐私

* `jsmediatags 3.9.5` 与 `color-thief 2.4.0` 已连同许可证内嵌到 `index.html`，本地播放、标签读取和主题取色不再依赖运行时 CDN。
* 公共搜索、专辑和歌手元数据可进入 IndexedDB 缓存；包含 `cookie`、`token`、`key` 等凭据参数的请求只使用账号内存缓存，登录状态、二维码和账号修改不缓存。
* 公共缓存键会移除 `timestamp`、`noCache` 等瞬态参数。启动迁移和退出登录都会清理旧版含敏感参数的 IndexedDB 键。
* 在线音频链接遇到 `401 / 403 / 404 / 410` 时只清理对应缓存并强制重新解析一次；其他错误不会无条件清缓存或无限重试。
* 页面隐藏时暂停视觉绘制；系统启用“减少动态效果”后，流体背景和非必要动画会停用。

### 播放模式与循环优先级

* **列表循环 / 随机播放**：歌曲播放到文件末尾后切换曲目。
* **单曲循环**：使用已解码的 `AudioBuffer` 在音频线程内直接从文件末尾回到 0，不重新下载、读取或解码歌曲。
* **A/B 循环优先**：开启循环设定或导入 `.sli` 后，自定义 A/B 点优先于单曲循环。A 点之前的前奏可以先播放一次，之后按 `B → A` 持续循环。
* **文件接缝说明**：播放器不会在循环边界额外插入停顿，但如果音频文件本身首尾带有静音、编码填充或波形不连续，这些内容仍会被原样播放。

### 方式三：智能循环制作

1. 点击 **「🔍 智能分析」** (移动端在高级菜单中)。
2. 播放到循环起点，点击 `📍 定位 A`；播放到终点，点击 `📍 定位 B`。
3. 点击 `✨ 开始精确匹配`，系统会自动计算最佳接缝。
4. 点击 `💾 导出 .sli` 保存你的劳动成果。

## 📦 部署说明

本项目为纯静态页面，但网易云功能及下载代理功能依赖后端 API。

### 1. 静态页面托管

上传单个 `index.html` 至 GitHub Pages、Vercel 或 Cloudflare Pages 即可。播放器所需的两个固定版本前端库已经内嵌，不需要额外上传 CDN 资源。

### 2. 后端 API 配置 (重要)

HTML 文件头部包含全局配置变量，请根据你的部署环境修改：

```html
<script>
    // 全局 API 地址 (网易云音乐 API)
    window.API_BASE_URL = 'https://musicproxy.minori.eu.cc';
    
    // 下载代理 Worker 地址 (用于绕过 Referer 限制下载音频)
    window.DOWNLOAD_PROXY_URL = 'https://proxy.minori0721.dpdns.org';
</script>

```

* **API_BASE_URL**: 依赖 [NeteaseCloudMusicApiEnhanced](https://github.com/neteasecloudmusicapienhanced/api-enhanced/) ，我的网页使用了经我小优化的 Fork 版本（ [api-enhanced](https://github.com/minori0721/api-enhanced)）。
* **DOWNLOAD_PROXY_URL**: 需要一个简单的 Cloudflare Worker 或 Nginx 反代，用于处理跨域和 Referer 头，以便浏览器能触发文件下载。

如果部署平台支持构建期环境变量，也可以不直接改业务代码，而是在平台里配置后由构建步骤注入到这两个变量：

* `API_BASE_URL`: 网易云音乐 API 地址。
* `DOWNLOAD_PROXY_URL`: 下载代理 Worker / 反代地址。

注意：Aurora Player 是纯前端静态页面，浏览器运行时不能直接读取服务器环境变量；环境变量需要在部署平台的构建流程里替换进 `index.html`，或通过平台提供的静态变量注入能力生成最终页面。

经我测试，国内可直连的部署服务包括：
~~[zeabur](https://zeabur.com/)~~（zeabur的serverless函数已经挂掉啦！） | [hugging face](https://huggingface.co/)

## 🔧 技术栈

* **Core**: HTML5, CSS3, ES6+ JavaScript
* **Audio**: Web Audio API (`AudioContext`, `AudioBuffer`)
* **UI Framework**: Vanilla CSS (Glassmorphism, Grid/Flexbox, Responsive)
* **Visualization**: HTML5 Canvas API
* **Libraries**:
* `jsmediatags 3.9.5`: ID3 标签解析，源码与许可证已内嵌
* `color-thief 2.4.0`: 图片颜色提取，源码与许可证已内嵌


* **Self-Developed Modules**:
* `NativeFlacParser`: 手写 FLAC 二进制解析器 (无需 Wasm)
* `YrcEngine`: 高性能逐字歌词渲染引擎，基于 CSS GPU 加速实现扫光动画
* `LoopAnalyzer`: 波形匹配与过零点分析算法
* `NeteaseIntegrator`: 网易云 API 完整对接模块 (包含 VIP 下载 Fallback 机制及智能格式判定)



## 🎯 算法细节

### 本地文件与 SLI 配对

本地文件导入采用“完整扫描 → 路径规范化 → 配对 → 解析 → 播放时应用”的流程。文件路径会统一分隔符、扩展名大小写和 Unicode 表示形式，并按以下顺序匹配：

1. 同目录且包含完整音频文件名，例如 `foo.wav` 对应 `foo.wav.sli`。
2. 同目录且基础名相同，例如 `foo.wav` 对应 `foo.sli`。
3. 跨目录回退仅在基础名全局唯一时使用；出现多个同级候选时标记为歧义，不自动猜测。

SLI 中的采样点会先保存到对应曲目，等音频解码并取得真实时长后再应用。换算规则保持为 `Start A = To / Base Rate`、`End B = From / Base Rate`；无 SLI、格式无效或超出音频时长的曲目不会继承上一首的循环状态。

### 循环点匹配 (Loop Matching)

智能循环匹配提供三档策略：

1. **速度优先**：保留旧版快速 MSE 匹配逻辑，适合 A/B 点已经比较准的场景。
2. **均衡推荐**：默认档，使用低采样率 NCC 粗筛，再回到原采样率进行接缝评分与精修。
3. **准确优先**：全筛候选与精修流程，覆盖更强但耗时更长。

评分会综合接缝前后波形相似度、跳变误差、循环长度偏差、粗点距离和风险项。默认档还会对低风险的临界候选做小幅置信度校准，提升可用结果的采纳率。

### 置信度 (Confidence)

算法会返回一个置信度评分：

* `>= 80%`：高置信度，自动应用并显示“找到了”。
* `>= 30%`：候选结果，自动应用但建议先试听确认。
* `< 30%`：不可靠结果，不自动应用，建议重新靠近 A/B 粗点。

## 📄 许可证

[MIT License](https://www.google.com/search?q=LICENSE) © 2025 Aurora Project
