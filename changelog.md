# Changelog

## v0.0.3 - 2026-08-13

- 网易云下一首预加载阈值调整为剩余约 15 秒，本地文件保持剩余约 10 秒。
- 复用已预热的网易云音频 URL 和完整音频结果，避免重复请求和重复解码。
- 修复手动点击已预加载目标时错误取消预加载的问题。
- 为失效的网易云音频地址增加单次强制重新解析，避免旧缓存持续复用。
- 恢复移动端顶部工具栏布局：「导入」「高级」使用长条胶囊按钮，云音乐、下载和播放列表使用圆形图标按钮。
- 统一移动端工具栏按钮内容居中，并保持主要触点不小于约 44px。
- 完成内联脚本语法检查、`git diff --check` 以及桌面和移动端浏览器回归检查。

## v0.0.2 - 2026-08-13

### 网易云歌手页请求与缓存优化

- 歌手首次进入优先调用 `api-enhanced` 的 `/artists?id=...` 聚合接口，一次取得歌手资料和热门歌曲；只有聚合响应缺字段或失败时才按需回退 `/artist/detail`、`/artist/top/song`。
- 歌手概览的专辑与 MV 请求统一预取 `limit=30&offset=0`，概览只展示前 6 项，切换到“专辑 / EP”或“MV”第一页时复用同一 URL 的缓存，避免重复请求。
- 删除歌手页专用的多套内存缓存和 in-flight Map，统一接入公共 JSON 缓存、请求去重、导航 AbortSignal 和成功响应缓存策略。
- 局部失败不再被错误结果永久缓存；概览中的专辑/MV 重试按钮只刷新失败的资源。动态关注状态读取失败时显示可重试状态，不会永久禁用关注按钮。
- 歌单后台加载到已加载队尾后，若后续批次失败、取消或全部结束，会正确清除等待态；有可用下一首时立即播放，没有可用下一首时保持暂停并给出原因。
- 修复专辑详情页渲染时缺少默认参数导致的 `options is not defined`；用户手动点选歌单中已加载歌曲时会清除旧的自动推进等待态，后台追加歌曲不会再抢走用户当前选择。
- 同一歌手在概览、专辑/EP 和 MV 标签间切换时保留可复用的在途请求，并由统一 JSON in-flight 去重；跨歌手或跨实体切换仍会取消旧导航请求。

### 本轮验证

- 只读核对 `api-enhanced` 当前 `d7c0db9`：`/artists`、`/artist/detail`、`/artist/top/song`、`/artist/songs`、`/artist/album`、`/artist/mv` 与动态接口均保持兼容；未修改 API 仓库。
- 使用固定延迟的 Chromium 模拟 API，各运行 5 次。当前版本相对旧版页面副本的结果如下，耗时单位为毫秒：

| 指标 | 旧版 | 当前版 |
| --- | ---: | ---: |
| 概览中位数 / P95 | 971 / 1147 | 950 / 1050 |
| 首次专辑标签 | 322 | 21 |
| 首次 MV 标签 | 312 | 8 |
| 重复专辑标签 | 8 | 9 |
| 概览请求数 | 7 | 4 |
| 快速跨歌手切换 | 1251 | 443 |
| 上下文数量（12 轮切换后） | 26 | 2 |

- 旧版失败重试没有发起新请求且无法恢复；当前版 MV 失败后请求数由 `1` 增至 `2`，页面恢复成功。
- 当前版 5 个内联脚本语法检查、`git diff --check` 和 Chromium 页面错误检查通过；模拟请求、快速切换、标签复用和局部重试均无控制台错误。
- 追加回归验证：专辑深链接、同歌手标签复用、跨歌手慢请求取消、歌单手动点歌清除等待态，以及 `1440x900`、`390x844`、`320x700` 三个视口均通过。

### 播放列表封面加载优化

- 将封面加载改为 URL 级资源管理：相同封面地址只发起一次请求，一个成功结果可回填到多个播放列表行。
- 保留 4 个并发槽，并按当前可见、预加载、失败重试排序；可见区域外只提前约 300px 加载，避免长歌单一次性抢占网络和解码资源。
- 单次封面请求增加 8 秒超时，失败最多重试 2 次；连续失败进入 30 秒冷却，且新的可见封面可以抢占旧的延迟重试任务。
- 歌单后台追加改为增量插入，不再整表重建；已显示的封面节点和已完成的 `src` 会保留，减少滚动时闪烁和重复解码。
- 增加播放列表代数、节点连接状态和资源请求令牌校验。快速替换歌单、重复歌曲详情回填或旧图片迟到完成时，不会把标题或封面写进新列表。
- 统一封面查询参数拼接，已有查询参数使用 `&param=80y80`，没有查询参数使用 `?param=80y80`。

### 封面专项验收

- 20 行共享 3 张封面：实际图片请求 `3` 次，去重后 URL `3` 个。
- 300 行长列表分 13 批追加：最终 `300` 行，本次运行单批追加最长约 `0.7ms`，首行封面节点保持连接。
- 失败封面：连续失败 `3` 次后进入冷却；新的可见封面可以抢占旧的延迟重试，不会被旧重试阻塞（本次运行约 `5ms` 开始请求）。
- 重复 `songId` 详情回填：`3/3` 行同步更新标题和封面；快速替换列表后旧列表无回填。
- Chromium 视口 `1440x900`、`390x844`、`320x700`：页面宽度与视口一致，播放列表无横向溢出，控制台无错误。
- 本节测试通过临时 Chromium 页面和模拟图片响应完成；耗时为本机单次运行值，不代表固定网络速度，临时脚本已删除。

### 播放列表恢复

- 播放列表持久化升级为 schema 3，使用 IndexedDB `player-state` 保存结构化歌曲信息，同时写入轻量 `localStorage` 快照，降低浏览器关闭或 IndexedDB 写入尚未完成时丢失列表的概率。
- 在线歌曲、本地歌曲元数据、播放顺序和当前索引都会保存；本地文件在浏览器提供文件句柄时会尝试自动恢复，没有永久句柄时会保留列表并提示重新选择文件。
- 增加旧 schema 2 状态兼容转换，并继续迁移更早的 `aurora_saved_playlist` / `aurora_saved_index` 数据；含有 ` - ` 的歌曲标题不会再被错误拆分。
- 使用写入队列和版本号避免快速切歌、刷新或连续更新时旧的 IndexedDB 写入覆盖较新的播放列表状态。

### 网易云歌单分批加载

- 歌单详情先读取完整 `trackIds`，再按 URL 安全长度拆分 `/song/detail` 请求，不再等待所有歌曲批次结束后才允许导入。
- 初始歌单信息中的首批歌曲加载完成后即可导入，例如 `10/120`；后续批次在后台按原始顺序追加，播放到已加载队列末尾时会等待下一批而不是立即误判为播放结束。
- 加载状态增加取消收尾：取消请求会停止后续网络和退避等待，保留已加载歌曲，并将按钮置为禁用的“已结束”，不会让已导入队列永久处于后台加载状态。
- 歌单加载令牌和队列引用继续隔离旧请求；快速从一个歌单切换到另一个歌单时，迟到响应不能追加到新歌单。

### 网易云 API 能力对齐

- “我的歌单”改为首屏先请求一页，依据 `/user/playlist` 的 `more` 和 `offset` 后台继续加载；按歌单 ID 去重，旧账号请求或迟到分页不会覆盖当前用户。
- 本地歌词搜索优先调用 `/search/match`，传递标题、专辑、歌手和秒级时长；接口失败、无候选或响应格式不完整时回退现有 `/cloudsearch` 匹配逻辑。
- 在线队列会批量预取后续约 3–5 首歌曲的 `/song/detail`，但仍只预热最可能的下一首音频 URL，不再提前下载多首整曲音频。
- 下载窗口打开时按需调用一次 `/song/music/detail`，用 `br`、`size`、`sr` 补充各音质行；真实下载链接仍由现有下载接口和 match 兜底逻辑确认。
- 登录状态完成后会重新触发“我的歌单”页面，避免深链接打开时登录请求与页面渲染竞态导致误显示“请先登录”。

### 文档与兼容边界

- README 补充了当前 API 的实际职责：完整 `trackIds`、`/song/detail` 批次、`/user/playlist` 分页、`/search/match` 秒级时长和 `/song/music/detail` 按需读取。
- README 明确说明本地匹配不会额外计算 MD5，接口无候选时仍回退 `/cloudsearch`；这可以避免把“匹配接口支持 MD5”误解成“播放器必须先扫描整首文件”。
- README 补充当前未实现的范围：播客/声音页面和 Aurora 内置 MV 播放器；MV 目前只跳转网易云官方页面。
- 文档中的验证结果区分了本地模拟 API、浏览器回归和真实网易云流程，未把未使用真实账号的操作写成已验证的账号操作。

### 本轮验证

- API 能力依据本地 `api-enhanced` 文档与模块实现核对，未修改或推送 API 项目。
- 本地 Chromium 模拟 API 回归通过：首屏歌单 `2` 项、后台 `offset=2` 追加并去重为 `3` 项；`/search/match` 成功和回退路径均通过；批量详情请求为 `1` 次且不包含当前歌曲；音质详情请求为 `1` 次。
- 模拟浏览器控制台无 error/warn；本轮未使用真实账号、真实收藏操作或真实音频下载。

### 验证结果

- 5 个内联脚本通过 `vm.Script` 语法检查，`git diff --check` 通过。
- 浏览器插件测试：慢速模拟歌单在 `10/120` 时即可导入，后台最终追加到 `120/120`；取消后保留 10 首且状态结束；快速切换 `111 → 222` 后页面只显示 222。
- 浏览器刷新后播放列表恢复成功，当前歌曲和队列顺序保持不变；Aurora 页面控制台无 error/warn。

## 2026-08-03

### 单文件依赖与缓存隐私

- 将固定版本的 `jsmediatags 3.9.5`、`color-thief 2.4.0` 及许可证内嵌到 `index.html`，移除运行时 CDN 脚本请求，继续保持纯静态单文件部署。
- IndexedDB 升级到 v2，在 `metadata` 之外增加 `player-state`；升级和启动时会删除旧版含 `cookie`、`token`、`key`、`csrf`、`music_u` 参数的缓存键。
- JSON 缓存分为 `public / account / none`：公共元数据可持久化，带凭据的请求自动降级为账号内存缓存，登录状态、二维码、账号修改和强制刷新不缓存。
- 公共缓存键移除 `timestamp`、`noCache` 等瞬态参数，账号缓存键不保存凭据原文。
- 新增 `authGeneration`、Cookie 快照和账号请求 AbortController；登录、二维码成功和退出后，旧账号响应不能恢复已失效的用户 UI。
- 退出登录会中止账号请求，清理账号内存缓存、动态状态、已解析音频 URL 和旧敏感 IndexedDB 数据。
- 在线音频 URL 仅在 `401 / 403 / 404 / 410` 时清除对应缓存并强制重新解析一次；第二次失败直接上抛，不无限重试。

### 播放状态与本地文件稳定性

- 播放核心统一为 `idle / loading / playing / paused / error`，播放图标、封面动画、加载提示、Media Session、迷你播放器和 ARIA 状态由同一入口更新。
- 切换本地或网易云曲目进入加载态时立即清空上一首的解码缓冲与时间轴，加载期间拖动进度条不会短暂重播旧曲。
- 修复随机模式只有一首歌曲时自然结束后留下错误播放状态的问题，改为复用已解码 `AudioBuffer` 重播本曲。
- FLAC 标签解析改为复用已经读取的 ArrayBuffer，所有 DataView 访问先做边界检查；截断元数据、异常注释和过大封面只忽略标签，不阻塞后续音频解码。
- 切歌时清空临时音质探针；解码失败会明确显示“无法播放”和错误原因，不再停在 `Parsing...`。
- 智能分析试听会验证音频、时间范围和当前曲目，停止定时器同时绑定播放令牌、曲目和音频节点，切歌后不能停止新音频。
- 进度条改用 Pointer Events 和 pointer capture；`pointercancel` 会保留最后拖动位置，并恢复手势开始前的播放或暂停状态。

### 弹层、历史与键盘交互

- 云音乐和下载窗口共用弹层控制器：背景 `inert`、Tab/Shift+Tab 焦点循环、Escape/背景/关闭按钮统一关闭，并恢复打开前焦点。
- 音乐路由历史项记录会话、内部深度、父项和滚动位置；面板返回只处理 Aurora 内部页面，根页返回关闭面板，浏览器前进仍可重新进入。
- 直接访问深链接、刷新、非法 ID 和未知路由都有稳定退路，不会从面板返回到 `about:blank`。
- 全局 Space、方向键快捷键只在非交互区域生效；输入框、按钮、选择器、链接、滑块和打开的弹层保留原生键盘行为。
- 播放列表行改为真实 `button`，支持 Enter/Space，并使用 `aria-current` 标识当前曲目。
- 补齐播放模式、上/下一首、播放、音量和面板按钮的动态 ARIA；移动端主要触点均不小于约 44px。

### 歌单、封面与状态恢复

- 长歌单使用一个全流程 AbortController，固定显示加载进度和取消按钮；基础信息、每个歌曲批次和退避等待都能单独超时或立即取消。
- 旧歌单批次由加载令牌隔离，取消或切换请求后，迟到响应不能覆盖当前结果；部分批次失败时保留已加载歌曲并禁止不完整导入。
- 歌单详情封面直接设置 `src`；批量歌曲封面继续使用共享 IntersectionObserver，动态创建后立即注册。
- 在线播放列表持久化改用结构化 IndexedDB 字段，不再从显示字符串拆分歌名；旧 `localStorage` 数据会迁移，并保留标题内部的 ` - `。

### 渲染与响应式

- 新增按需 Render Scheduler：暂停且无特效时停止 RAF，无特效播放约 15Hz 刷新时间和歌词，视觉特效使用流畅 RAF。
- 无特效时 Canvas 只清空一次；页面隐藏后取消帧与低频定时器，恢复时立即同步进度。
- 支持 `prefers-reduced-motion`，停用流体背景、唱片旋转、逐字填充和非必要过渡。
- 桌面主网格改为可收缩轨道，`901–1150px` 自动缩小封面列；关闭的桌面面板改用视口内淡出/缩放，消除屏外 transform 造成的真实横向空白。
- 手机封面和唱片继续保持 1:1，歌词按内容视口居中，顶部图标按钮补足 44px 点击区域。

### 验证结果

- 当前 5 个内联脚本通过 `vm.Script` 语法检查；缓存键、路由、播放状态、FLAC 边界、旧播放列表迁移和 URL 单次刷新纯函数测试通过。
- 真实导入 `bgm_10`、`bgm_11` 和对应 SLI，结果为 `2 音频 / 2 SLI / 匹配 2 / 未匹配 0 / 歧义 0 / 无效 0`；循环点分别为 `18.250 → 124.250`、`9.712 → 129.712`。
- 连续快速切歌 20 次后曲目、活动行、循环点和播放状态一致；切到无 SLI WAV 时自定义循环关闭，单首随机播放、试听后切歌、Pointer Cancel 和损坏 FLAC 错误状态均通过。
- 真实网易云流程已走通“综合搜索 → Beyond 歌手 → 专辑 → 播放 → 下一首 → 返回/前进”，上下文队列、迷你播放器和播放不中断正常。
- 模拟 150 首慢歌单取消后补回 3 个迟到批次，页面仍保持“请求已取消”；模拟单批 HTTP 400 时保留 `70/120` 首并显示不完整警告。该 HTTP 400 是测试注入的预期资源错误，没有应用脚本异常。
- 假凭据 IndexedDB 键在重载后被删除，公共测试键保留，v2 的 `metadata / player-state` 均存在；未登录收藏只进入扫码页，未发送 `/album/sub` 或 `/artist/sub` 请求。
- 隔离上下文中的延迟登录状态响应在退出后被丢弃，假 Cookie 已删除且账号栏保持隐藏；模拟首个音频 URL 返回 403 时，解析接口调用正好 2 次、旧/新音频各请求 1 次并成功播放。
- `1440×900`、`1024×768`、`390×844`、`320×700` 下文档宽度等于内容视口，封面/唱片比例为 `1.000`；手机主要控件均达到至少 `44×44px`。
- 三轮两秒空闲采样中，基线 `af8834b` 的 TaskDuration 中位数约 `0.1066s`，当前约 `0.0887s`；更稳定的变化是 RAF/Canvas 清屏中位数由 `127/127` 降为 `0/0`。无特效播放实测约 `12 RAF/s`，雪、频谱、波浪均保持约 `68–72 RAF/s` 且 Canvas 非空。
- 页面隐藏事件测试期间新增 RAF 为 `0`，恢复后重新绘制；减少动态模式下只执行一次同步帧，背景动画为 `none`。
- 测试环境没有可用网易云登录状态，因此没有执行真实账号收藏/关注写操作。

## 2026-07-16

### 网易云音乐内容浏览器

- 将原有歌曲搜索弹窗升级为桌面宽屏、移动端全屏的内容浏览器，保留扫码登录、音质选择、歌单导入和我的歌单。
- 新增「综合 / 歌曲 / 专辑 / 歌手」分类搜索；综合搜索并行请求三类稳定接口，不依赖当前返回空结果的 `/cloudsearch?type=1018`。
- 新增专辑详情页，展示封面、年份、简介、歌手、收藏数和完整曲目，并支持顺序播放、随机播放及账号收藏/取消收藏。
- 新增歌手详情页，展示简介、关注状态、热门歌曲、全部歌曲、专辑/EP 和 MV；MV 使用安全的新窗口链接打开网易云官方页面。
- 歌曲行中的封面、专辑名和每个歌手名均可独立跳转；主播放页的在线歌曲封面和歌手名也接入同一导航。

### 路由、队列与播放联动

- 新增 `#music/search`、`#music/album/:id`、`#music/artist/:id` 和歌单页面 Hash 路由，支持深链接、刷新、浏览器前进/后退、分页与滚动位置恢复。
- 关闭内容浏览器不会停止音乐，并会记住最后浏览页和最后搜索条件；再次打开时自动恢复。
- 从综合搜索、分类搜索、专辑和歌手页面播放歌曲时，会建立对应的网易云上下文队列，并继续复用原有音频解锁、歌词、音质、预加载和播放令牌。
- 新增内容浏览器底部迷你播放器，提供当前歌曲、歌手入口、播放/暂停和下一首；移动端适配安全区并预留滚动空间。
- 页面请求统一使用导航令牌和 AbortController，快速切换时旧搜索、详情和分页响应不能覆盖新页面。
- 播放进度填充改用固定圆角的 `clip-path` 裁剪，避免 `scaleX()` 同时压缩圆角而导致进度条左端偶尔看起来是平的；更新过程仍不修改布局宽度。

### 账号与兼容处理

- 专辑收藏使用 `/album/sub`，歌手关注使用 `/artist/sub`；账号写操作不进入缓存，成功后立即刷新动态状态。
- 未登录时点击收藏或关注会进入现有扫码页面，登录后返回原内容页，但不会自动执行修改账号状态的操作。
- 统一兼容 `ar/al` 与 `artists/album` 字段，并为歌曲、专辑和歌手建立规范化数据模型。
- 保持单文件、纯静态部署和零新增依赖；搜索缓存 5 分钟，详情与分页数据缓存 30 分钟，账号动态状态使用 15 秒内存缓存。

### 验证结果

- 内联脚本语法检查和 `git diff --check` 通过。
- 实测歌曲、专辑、歌手搜索，以及专辑详情/动态、歌手详情/动态/歌曲/专辑/MV 接口均正常返回。
- Chromium 已走通「搜索 → 歌手 → 专辑 → 建立 10 首专辑队列 → 下一首」，并验证深链接刷新、分类分页、前进/后退、滚动恢复、关闭后恢复和未登录扫码门槛，控制台无 error/warn。
- 连续快速切换 20 个搜索、专辑和歌手路由后，最终 URL 与页面标题一致，没有旧响应覆盖新页面。
- `1440×900`、`1024×768`、`390×844`、`320×700` 下内容面板无内部横向溢出；移动端专辑封面保持正方形，迷你播放器不遮挡内容。
- 测试环境没有可用网易云登录状态，因此没有修改真实账号收藏/关注；登录门槛和二维码生成已验证。

## 2026-07-12

### 文件夹导入与伴随文件配对

- 文件夹拖放改为递归读取：优先使用 `getAsFileSystemHandle()`，回退到 `webkitGetAsEntry()`，最后兼容普通 `FileList`。
- 导入前会完整收集并去重文件，扫描完成后再原子替换播放列表，避免边扫描边播放造成列表和伴随文件状态不完整。
- 新增持久的 SLI/LRC 注册表，支持 `foo.sli`、`foo.wav.sli`、`foo.ogg.sli`、大小写混合扩展名及 Unicode 文件名。
- 配对顺序固定为“同目录完整文件名 → 同目录基础名 → 全局唯一基础名”；同级多候选会标记为歧义，不自动猜测。
- 包含音频的新批次会替换播放列表并清理旧伴随文件；仅 SLI/LRC 的批次会补充并重新匹配整个当前列表。
- 空列表时先导入的 SLI/LRC 会作为一次性待配对文件暂存，下一批本地音频导入后自动使用。
- 单独补拖同名 SLI 时会替换旧绑定，避免浏览器无法提供原目录路径时旧文件继续抢占优先级。

### SLI 应用与播放竞态修复

- SLI 解析改为纯数据流程，只保存 `From` / `To` 采样点；音频解码完成后再按当前 Base Rate 和真实时长验证并应用。
- 允许一个采样点级别的解码边界误差，明显超过音频时长的循环点会被拒绝，不会擅自大幅裁剪。
- 切歌时立即清除上一首的自动 SLI 循环，普通曲目不会继承上一首的循环点或 `SLI Loaded` 状态。
- 本地解码、标签、LRC、网易云音频、歌词、重试流程统一加入播放加载令牌，过期任务不能覆盖当前音频、循环、歌词、封面或 UI。
- 统一网易云请求的 AbortController，切换本地文件或替换播放列表时可以取消旧在线音频请求。

### 导入状态与诊断

- 播放列表为有效 SLI 显示 `∞` 标记，为无效或歧义项显示警告标记和原因提示。
- 新增可见导入汇总，显示“音频 / SLI / 匹配 / 未匹配 / 歧义 / 无效”数量；Debug 同时记录具体文件路径与失败原因。
- 扫描期间显示“正在读取文件夹”，浏览器无法展开目录时不再静默导入不完整内容。

### 验证结果

- 真实原声带目录：`34` 首 WAV、`5` 个 SLI，结果为 `34 音频 / 5 SLI / 匹配 5 / 未匹配 0 / 歧义 0 / 无效 0`。
- 五组循环点解析为约：`9.409 → 114.136`、`0.381 → 85.805`、`2.182 → 72.000`、`12.750 → 120.750`、`26.325 → 141.525`。
- 实播 track 07 后自动启用 `9.409 → 114.136`，切换到无 SLI 的 track 08 后循环正确关闭；track 11 自动启用 `0.381 → 85.805`。
- 覆盖整目录导入、音频后补 SLI、SLI 先暂存、双扩展名优先、同名歧义、单独替换 SLI、两套递归目录接口和快速连续切歌。
- 静态语法检查、`git diff --check` 通过；Chromium 页面控制台为 `0 errors / 0 warnings`。

## 2026-07-11

### 单曲无缝循环

- 单曲循环由“播放结束后通过 `onended` 创建新节点重播”改为 `AudioBufferSourceNode.loop` 内部整曲循环，循环范围为 `0 → audioBuffer.duration`。
- 单曲循环复用已解码的 PCM 数据，不重新请求、读取或解码歌曲，避免主线程回调调度产生的循环间隙。
- A/B 循环和 `.sli` 循环点保持更高优先级；启用自定义循环时继续按 `loopEnd → loopStart` 回跳。
- 单曲循环的进度、歌词时间改为按歌曲长度取模，跨越多轮后仍能正确回到对应时间。

### 极端状态修复

- 切换列表、随机、单曲模式时保留当前播放位置，只在进入或退出单曲循环时重建音频节点。
- 进入单曲循环时取消下一首预取，释放已经缓存或正在下载的下一首数据。
- 修复拖动到歌曲精确末尾时以 `offset === duration` 启动并立即结束的问题。
- 对负数、`NaN`、`Infinity` 和超过歌曲长度的播放位置进行安全归一化。
- 统一 A/B 区间校验，拒绝 `A >= B`、负数或 B 超过歌曲长度的配置，避免复选框状态与实际播放不一致。
- 播放过程中应用或关闭 A/B 循环会立即更新当前音频节点；关闭后会按当前播放模式恢复普通播放或整曲单曲循环。

### 验证结果

- Chrome 内存音频状态矩阵：`37/37` 项通过，无控制台 error/warn。
- 覆盖列表/随机/单曲切换、暂停恢复、手动上下曲、A/B 优先级、无效区间、末尾拖动、异常 `onended`、下一首预取和连续 60 次快速切换。
- 1-sample 极短音频可正常进入整曲循环。
- OfflineAudioContext 连续渲染三轮 PCM，两个循环边界最大误差为 `0`，未插入空白采样。
- 静态检查：提取 `index.html` 内联脚本执行 `node --check`，通过。
- 空白检查：`git diff --check`，通过。

## 2026-07-08

### 网易云播放兼容

- 新增 WebAudio 解锁流程，在网易云歌曲播放开始时立即尝试 `AudioContext.resume()` 并播放极短静音 buffer，降低 iOS Safari 点击网易云歌曲后无声的概率。
- 网易云播放列表切歌时会在请求歌曲详情、音频 URL 和解码之前先尝试解锁音频上下文，避免异步链路丢失用户手势。
- `startAudio()` 增加兜底检查：如果浏览器仍拦截音频启动，会停止刚创建的播放节点并把 UI 状态退回暂停，避免出现“看起来在播但没声音”。
- 播放按钮复用同一套解锁流程，便于 iPhone 用户在首次被拦截后再次点击恢复播放。

### 动画与移动端体验

- 播放进度条改用 `transform: scaleX()` 更新，减少播放过程中的布局写入。
- 歌词高亮改用 transform 放大，并缩小过渡属性范围，减少歌词滚动时的重排压力。
- YRC 逐字歌词动画重启改为批量延后到 `requestAnimationFrame`，避免逐字强制回流。
- 缩小按钮、音量浮层、高级菜单等组件的 transition 范围，减少不必要的样式计算。
- 修复手机端 active 歌词在居中布局下因左侧 transform origin 导致视觉偏右的问题。

### 验证结果

- 静态检查：提取 `index.html` 内联脚本执行 `node --check`，通过。
- 空白检查：`git diff --check`，通过。
- 移动端歌词居中验证：Chrome headless/CDP 在 `390x844`、`320x700` 视口下 active 歌词中心偏差均为 `0px`，无控制台 error/warn。

## 2026-06-23 歌词解析优化

### 网易云歌词兼容

- 支持解析网易云 `{ "t": ..., "c": [...] }` JSON 行歌词格式，将 `c[].tx` 自动拼接成显示文本。
- `t >= 0` 的 JSON 行会按毫秒时间戳转为同步歌词，参与正常歌词滚动。
- `t < 0` 的 JSON 行会作为纯音乐/无歌词信息处理，不再被误计为普通歌词行。
- 当歌曲只有 `t < 0` 信息时，歌词区显示“🎵 纯音乐，无歌词”，并在下一行显示作曲等附加信息。
- 歌词解析日志增加 `Meta` 计数，便于区分真实歌词行和无时间轴信息行。

### 验证结果

- 静态检查：提取 `index.html` 内联脚本执行 `node --check`，通过。
- 空白检查：`git diff --check`，通过。
- 解析抽查：`{"t":-1,"c":[{"tx":"作曲: "},{"tx":"solfa"}]}` 会进入 `Meta`，不再进入同步歌词数组。

## 2026-06-23 后续优化

### 智能循环匹配

- 默认“均衡推荐”增加 Web Worker 后台计算，点击“开始精确匹配”后不再长时间阻塞主界面交互。
- Worker 会复用当前歌曲的单声道分析数据，重复匹配同一首歌时减少重复准备开销。
- 切歌或重新加载音频时会取消旧分析任务并释放 Worker 内的旧音频数据，避免旧结果覆盖新歌。
- 默认档新增“安全候选加分”：只对长度接近、移动距离小、风险低、后段相位稳定且接近黄色阈值的候选小幅提升置信度，提高可用候选的采纳率。
- 候选排序加入风险项，降低后段相位不稳定或风险较高的候选抢占最终结果的概率。

### 移动端界面

- 修复部分手机分辨率下主封面被拉伸成非正方形的问题。
- 修复默认 CD 圆盘在窄屏布局下跟随变形成椭圆的问题。
- 主封面容器改用 `aspect-ratio: 1 / 1`，并在移动端使用更稳定的 `min()` 宽度约束。

### 验证结果

- 静态检查：提取 `index.html` 内联脚本执行 `node --check`，通过。
- Worker 源码拼接语法检查：通过。
- 空白检查：`git diff --check`，通过。
- 本地页面 smoke：`Aurora Player` 正常加载，智能匹配控件存在，无控制台 error/warn。
- 移动端尺寸抽查：`320px`、`375px`、`390px` 宽度下主封面和默认 CD 均保持正方形。

## 2026-06-23

### 智能循环匹配

- 在 Loop Analyzer 面板增加“匹配算法”选择器，默认使用“均衡推荐”。
- 新增三档智能匹配入口：
  - “速度优先”：保留旧版快速 MSE 匹配逻辑，适合 A/B 点已经比较准的场景。
  - “均衡推荐”：使用 NCC 粗筛加接缝评分精修，作为默认推荐档。
  - “准确优先”：保留全筛候选与精修流程，作为准确率优先的对照档。
- 统一三档算法返回结构，便于 UI 展示、日志记录和测试对比。
- 统一 UI 置信度显示：
  - `>= 80`：绿色“找到了”。
  - `>= 30`：黄色“候选，建议试听”。
  - `< 30`：红色“不可靠”。
- 默认“均衡推荐”增加精修提前停止逻辑：在已经找到稳定高置信候选时跳过后续昂贵精修，保持准确率的同时降低耗时。

### 稳定性修复

- 修复接缝评分里的 `seamJumpError` 计算，避免重复使用同类误差导致评分失真。
- 智能匹配运行中会锁定“开始精确匹配”按钮，避免重复点击导致并发分析。
- 切换播放位置时更完整地停止并断开旧的 `AudioBufferSourceNode`，降低重复播放节点残留风险。
- `.sli` 解析改为分别读取 `From` / `To` 字段，减少格式差异导致的导入失败。

### 验证结果

- 静态检查：提取 `index.html` 内联脚本执行 `node --check`，通过。
- 空白检查：`git diff --check`，通过。
- 两首测试曲 `bgm_10`、`bgm_11`，共 40 个默认档用例：
  - 优化前：`usefulAccepted 35/40`，误采纳 `0`，平均耗时约 `1475ms`，P95 约 `1744ms`。
  - 优化后：`usefulAccepted 35/40`，误采纳 `0`，平均耗时约 `1228ms`，P95 约 `1264ms`。
